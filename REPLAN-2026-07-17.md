# REPLAN 2026-07-17 — MCP v4 consolidation (this repo's slice)

Master plan: `gammarips-engine/docs/EXEC-PLANS/2026-07-17-simplification-replan.md`
(RATIFIED). Goal: 29 tools → **9**, breaking v4 release. Before deploying, re-check
current keyholder impact in the usage analytics (internal) and coordinate any needed
notice.

## Ratified tool map
| v4 tool | tier | absorbs (current handlers) |
|---|---|---|
| `get_pool` | free preview / pro full | get_overnight_signals, get_enriched_signals, get_pool_features, get_freemium_preview |
| `get_signal` | pro | get_signal_detail, get_earnings_window |
| `get_liquidity` | pro | get_pool_liquidity, get_contract_snapshot |
| `query_outcomes` | pro | query_outcomes, get_outcome_summary, get_opportunity_surface, get_harvest_curve, estimate_exit_rule, get_signal_performance, get_win_rate_summary, get_position_history, get_historical_performance (`view=` modes) |
| `replay_contract` | pro | get_contract_marks (`granularity="day"\|"minute"`) |
| `get_regime_context` | free | unchanged |
| `get_market_calendar_status` | free | get_available_dates |
| `get_playbook` | free | list_playbooks (no-arg lists); get_signal_explainer field dict + get_enriched_signal_schema become playbook pages |
| `get_daily_report` | free | get_report_list (no date arg lists recent) |

KILL: `web_search`. Do NOT touch auth/metering/rate-limit plumbing (verified working).

## Steps
1. **Shared data layer**: new `src/utils/data.py` — one BQ client, one Firestore client,
   all table constants. Today each module makes its own client with hardcoded project id
   (`contract_history.py:60`, `market_snapshot.py:70`, `historical.py:29`, `substrate.py:44`,
   `reports.py:16`); `_MINUTE_PATHS_TABLE` is defined twice.
2. **Merged handlers** reusing existing query logic. FastMCP derives schemas from
   signatures+docstrings — no schema files to migrate. Keep old handlers until tests green,
   then delete.
3. **Registry**: rewrite `_ALL_TOOLS` (`src/server.py:79-121`), `_DEFAULT_ANON_TOOLS`
   (`src/utils/auth.py:80-91` — free = pool/regime/calendar/playbook/report), and the 3
   prompts (`src/server.py:132-178`) that name tools in prose.
4. **Version + listings**: `SERVER_VERSION` → "4.0.0" (`src/server.py:41`); fix `mcp.json`
   tools array (currently stale at 25); `README.md:21-59`; `SECURITY.md` clamp table (:60);
   `pyproject.toml:3` optional. Server card auto-reflects. Republish official registry
   listing `io.github.DevDizzle/gammarips` as 4.0.0.
5. **Tests**: update hardcoded names in `tests/test_phase2_auth.py:131-184`,
   `test_v3_smoke.py`, `verify_tools.py`; run pytest locally — CI only runs ruff.
6. **Ship**: gammarips-review audit → `scripts/deploy.sh` (watch: `--set-env-vars`
   REPLACES the env set) → live verify: server card shows 9, anon pro-call →
   `subscription_required`, keyed calls return data on all 9.
7. **Downstream (other repos, after this freezes)**: webapp /developers + public/ agent
   files; trader skills/TOOL-MAP rename pass; public harness repo written against v4.

Stale artifacts — ignore: `openapi_schema.txt`, `tests/test_scenarios.json` (legacy GPT
surface), `mcp_config.json` (legacy /sse).

## Checklist
- [x] data layer hoisted (`src/utils/data.py` — one BQ + one FS client, all table
      constants; duplicate `_MINUTE_PATHS_TABLE` collapsed to one source)
- [x] 9 handlers built (`src/tools/v4.py`), removed from the registry/surface
      (impls retained as reused, leakage-audited query logic the handlers delegate to)
- [x] registry/anon-set/prompts rewritten (`server.py` `_ALL_TOOLS`, `auth.py`
      `_DEFAULT_ANON_TOOLS` = 5 free, 3 prompts)
- [x] versions + mcp.json/README/SECURITY synced (SERVER_VERSION/pyproject/server.json 4.0.0)
- [x] tests updated + passing (pytest 3 passed; smoke 49/52 + 3 credential-skips; ruff clean)
- [ ] reviewed + deployed + registry republished + live-verified
